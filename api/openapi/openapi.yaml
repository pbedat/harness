openapi: '3.0.3'
info:
  title: harness API
  version: '0.1'
servers:
  - url: https://localhost:3232/v1
paths:
  /kanban/boards:
    post:
      responses:
        '201':
          description: Created
  /kanban/boards/{boardId}/columns:
    post:
      responses:
        '201':
          description: Created
